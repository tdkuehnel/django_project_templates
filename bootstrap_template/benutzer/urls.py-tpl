from django.conf.urls import url
from django.urls import path

from . import views

app_name = 'benutzer'

urlpatterns = [
    path('anmeldung/',                                        views.{{ project_name|capfirst }}LoginView.as_view(),             name='anmeldung'),
]
